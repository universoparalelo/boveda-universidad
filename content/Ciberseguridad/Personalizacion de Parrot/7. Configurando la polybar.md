- Descargamos el repo
```
git clone https://github.com/VaughnValle/blue-sky.git
cd blue-sky
cd polybar
cp -r * ~/.config/polybar
echo '~/.config/polybar/./launch.sh' >> ~/.config/bspwn/bspwnrc
cd fonts # ~/blue-sky/polybar/fonts
sudo cp * /usr/share/fonts/truetype
fc-cache -v
```