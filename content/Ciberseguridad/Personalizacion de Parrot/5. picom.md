
- Mas librerias tochas para instalar
```
apt install meson libxext-dev libxcb1-dev libxcb-damage0-dev libxcb-xfixes0-dev libxcb-shape0-dev libxcb-render-util0-dev libxcb-render0-dev libxcb-composite0-dev libxcb-image0-dev libxcb-present-dev libxcb-xinerama0-dev libpixman-1-dev libdbus-1-dev libconfig-dev libgl1-mesa-dev libpcre2-dev libevdev-dev uthash-dev libev-dev libx11-xcb-dev libxcb-glx0-dev
```

- Repositorio de picom
```bash
cd Descargas/
git clone https://github.com/ibhagwan/picom.git
cd picom
git submodule update --init --recursive
meson --buildtype=release . build
ninja -C build
sudo ninja -C build install
```

- Instalar rofi
```bash
apt install rofi
rofi -show run
nano ~/.config/sxkhd/sxkhdrx
```
- Configuramos rofi
```conf
super + d 
	rofi -show run
```

- Instalamos bspwn
```
apt install bspwn
```

- Para  reiniciar la compu
```
kill -9 -1
# reinicia todos los procesos == reinicio
```

- Aca deberiamos elegir bspwn como escritorio pero tranquilos que no hay nada, es oscuridad pura aqui